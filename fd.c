#include <stdio.h>

// declare a matrix struct or just use a single array ?

int main(int argc, char* argv[])
{
    // read in a matrix
    // generate a matrix 
    printf();
}

// what does the frequent directions algorithm actually 
// note we are doing batched version
int * fd_sketch(float* mat, int l)
{
    /**
     * mat is input matrix size (n X m) row order
     * l is target dimension (l X m size of sketch)
     * */
    // should I modify in place or make a copy?     
    // get num of rows , columns for the matrix 
    // n, m =  
    // 
    // maybe easier to look at the paper? because it has SVD's and things 
    return mat;
}
